class Solution {
    public int[] twoSum(int[] nums, int target) {
        HashMap<int,int> a=new HashMap<>();
        for(int i=0;i<nums.length();i++){
            a[num[i]]=target-nums[i];
            if()
        }
    }
}